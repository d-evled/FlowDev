PORT=8080
HOST=0.0.0.0
LOG_LEVEL=info
# Set to false to make /ready return 503 (draining or dependency failure simulation)
READY=true

# Optional: baked into Docker images by CI (see Dockerfile ARG / deploy workflow)
# GIT_SHA=
# BUILD_TIME=
